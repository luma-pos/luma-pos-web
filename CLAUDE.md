# LumaPOS — Project context for Claude Code
- Source of truth for product logic & design: docs/master-prompt.md
- New design references: design/
- Stack: Next.js (web). 
- Commands: dev `npm run dev` · build `npm run build` · lint `npm run lint`
- Rules: follow the master-prompt design system (Be Vietnam Pro, warm neutrals, 16px radius,
  Part 3.5 taste dials), bilingual EN/VI labels, no banned defaults. Reuse components,
  don't fork. Run build + lint before considering a task done.